Flask>=3.0
# Production WSGI server
gunicorn>=20.1
# Optional — only needed for a real LDAP directory:
# ldap3>=2.9
# Optional — only needed for real Google OAuth login:
# Authlib>=1.3
# requests>=2.31
